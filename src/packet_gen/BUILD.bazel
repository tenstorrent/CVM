load("@cvm_pypi//:requirements.bzl", "requirement")

py_binary(
    name = "packet_gen",
    srcs = ["packet_gen.py"],
    deps = [
        requirement("Mako"),
        "//src/topology:topology_query"
    ],
    data = glob(["templates/template.*"]),
    visibility = ["//visibility:public"],
)
