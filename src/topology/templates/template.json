{
%for idx, location in enumerate(topo.locations):
  "${location.name}" : {
    "count" : ${len(location.instances)}\
  %if len(location.attrs) != 0:
,
  %else:

  %endif
  %for attr_idx, attr in enumerate(location.attrs):
    %if attr.value.isnumeric():
    "${attr.name}" : ${attr.value}\
    %endif
    %if attr_idx != len(location.attrs) - 1:
,
    %endif

  %endfor
  }\
  %if idx != len(topo.locations) - 1:
,
  %else:

  %endif
%endfor
}
