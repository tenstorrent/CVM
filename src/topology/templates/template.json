{
%for idx, location in enumerate(topo.locations):
  "${location.name}" : {
    "count" : ${len(location.instances)}
  %if len(location.attrs) != 0:
\
,
  %endif
  %for idx, attr in enumerate(location.attrs):
    %if not attr.value.isnumeric():
    "${attr.name}" : "${attr.value}"
    %else:
    "${attr.name}" : ${attr.value}
    %endif
    %if idx != len(location.attrs) - 1:
\
,
    %endif
  %endfor
  }\
  %if idx != len(topo.locations) - 1:
,
  %endif
%endfor

}
