{
%for idx, (name, attrs)  in enumerate(topo.attrs.items()):
  "${name}" : {
  %for attr_idx, attr in enumerate(attrs):
    %if attr.value.isnumeric():
    "${attr.name}" : ${attr.value}\
    %endif
    %if attr_idx != len(attrs) - 1:
,
    %endif
  %endfor
  }\
  %if idx != len(topo.attrs) - 1:
,
  %else:

  %endif
%endfor
}
