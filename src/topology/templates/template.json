{
%for idx, location in enumerate(topo.locations):
  "${location.path}" : {
  %for attr_idx, (name, value) in enumerate(location.attributes):
    %if value.isnumeric():
    "${name}" : ${value}\
    %endif
    %if attr_idx != len(location.attributes) - 1:
,
    %endif
  %endfor
  }\
  %if idx != len(topo.locations) - 1:
,
  %else:

  %endif
%endfor
}
