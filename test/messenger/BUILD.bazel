load("@rules_verilator//verilator/internal:versions.bzl", verilator_version = "DEFAULT_VERSION")
load("//test:test.bzl", "linkopts")

cc_test(
    name = "messenger_test",
    size = "small",
    srcs = ["test.cpp"],
    deps = [
        "//:messenger",
        "@com_google_googletest//:gtest_main",
        "@verilator//:libverilated".format(verilator_version),
    ],
    linkopts = linkopts,
)

