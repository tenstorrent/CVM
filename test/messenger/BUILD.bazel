load("@rules_verilator//verilator/internal:versions.bzl", verilator_version = "DEFAULT_VERSION")

cc_test(
    name = "messenger_test",
    size = "small",
    srcs = ["test.cpp"],
    deps = [
        "//:messenger",
        "@com_google_googletest//:gtest_main",
        "@verilator_v{}//:libverilator".format(verilator_version),
    ],
    linkopts = ["-lz"],
)

