# Generated by src/replay/replay_ports_gen.py -- do not edit.
# Every port of elsif_ports as its source declares it, so this spec
# describes the module rather than one configuration of it.
elsif_replay:
  dut: elsif_ports
  clock: clk
  exclude:
  - rst_n
  ports:
    mode_x:
      dir: in
      type: logic [3:0]
      when:
      - MODE_X
    mode_y:
      dir: in
      type: logic [7:0]
      when:
      - '!MODE_X'
      - MODE_Y
    mode_none:
      dir: in
      type: logic
      when:
      - '!MODE_X'
      - '!MODE_Y'
    out:
      dir: out
      type: logic
